.row
  .small-12.columns
    %h2 #{fa_icon 'users'} My Groups 
    %hr
    .row
      .small-6.columns
        = link_to 'New Group', new_group_path, class: 'button success radius small'
      .small-3.columns
      .small-3.columns
        = text_field_tag :search, 'search', class: 'radius'

      =render 'groups_table' if @groups
      =render 'shared/no_objects_message', :object => 'groups' if @groups.empty?